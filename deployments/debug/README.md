# Usage

Rename `launch.default.json` to `launch.json` and `tasks.default.json` to `tasks.json`

Run visual studio code Run and Debug "Launch Hospital & Clinic Applications" to start a debugging session.

URLs:
- Hospital EHR (task sender): http://localhost:8081/ehr
- Clinic EHR (task receiver): http://localhost:8082/viewer
